import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Python API functions are in /api/ root directory
  // Vercel routes them separately from Next.js
};

export default nextConfig;
